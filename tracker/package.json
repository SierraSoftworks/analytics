{
  "name": "@analytics/tracker",
  "version": "0.1.0",
  "private": true,
  "description": "The privacy-preserving tracking beacon embedded by the analytics agent and served at /tracker.js.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "jsdom": "^26.0.0",
    "vitest": "^4.1.8"
  }
}
